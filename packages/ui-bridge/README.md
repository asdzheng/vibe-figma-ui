# Archived V1 Package

`@vibe-figma/ui-bridge` is archived legacy code from the V1 bridge-first architecture.

Do not use it for new work. The active runtime path is:

- `packages/plugin`
- `packages/cli`

This directory remains in the repository only as historical reference while older notes are cleaned up.
