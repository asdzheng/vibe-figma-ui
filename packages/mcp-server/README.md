# Archived V1 Package

`@vibe-figma/mcp-server` is archived legacy code from the V1 bridge-first architecture.

Do not use it for new work. MCP is deferred in the current V2 CLI-first path, which centers on:

- `packages/plugin`
- `packages/cli`

This directory remains in the repository only as historical reference while older notes are cleaned up.
