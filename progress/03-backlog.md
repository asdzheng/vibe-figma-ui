# Backlog

## CLI Companion

- Add native raster screenshot output only if pixel-oriented review becomes worth the added cost beyond the existing SVG and HTML preview paths.

## Compatibility

- Add a thin MCP adapter only if a later compatibility requirement proves it is worth the cost.

## Fixtures And Testing

- Add more golden fixtures for full page-level capture beyond the current large-selection optimization sample.
- Expand live smoke assertions around specific node families once their runtime extraction is implemented.

## Deferred Optimization

- Consider a schema-native emitter for large page-level captures only if the current shorthand converter no longer gets the next useful size reduction.
- Consider reconnect-safe in-flight capture handoff only if session churn becomes a recurring real desktop workflow cost beyond the current `--session` targeting path.
- Consider second-level interning for repeated instance override payloads only if the simpler V3 schema cuts still do not meet the size budget.
- Consider a derived transport-minified export only after the canonical schema is structurally small enough.
